#include <stdio.h> // Arithamatic instruction
#include<math.h>
int main()
{
    // int a = 15;
    // float b = 5.6;      float constant

    // printf("The value of a+b is :%f\n", a + b);
    // printf("The value of a-b is :%f\n", a - b);
    // printf("The value of a*b is :%f\n", a * b);
    // printf("The value of a/b is :%f\n", a / b);

    int a = 15; // integer constant
    int b = 5;
    printf("The value of a+b is :%d\n", a + b);
    printf("The value of a-b is :%d\n", a - b);
    printf("The value of a*b is :%d\n", a * b);
    printf("The value of a/b is :%d\n", a / b);

    // int z;
    // z = b * a; // legal
    // //b * a= z; //Illegal
    // printf("The value of z is: %d\n", z);

    // printf("5 when divided by 2 leaves a reminder of %d\n", 5 % 2);
    // // printf("-5 when divided by 2 leaves a reminder of %d\n",-5%2);
    // // printf("5 when divided by -2 leaves a reminder of %d\n",5%-2);

    // // no operater is assumed to be present

    // // printf("The value of 4*5 is %d\n", (4)(5));  will not return 20/wrong!!  
    // printf("The value of 4*5 is %d\n",(4)*(5));

    // // there is no operation to perform exponention in c
     
    //  printf("The value of 4 to the power 5 is %f\n",pow(2,5));  // %f because it return double

    return 0;
}