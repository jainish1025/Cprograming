#include <stdio.h>

int main()
{
    int principal=200,rate=10,years=4;
    int simpleIntrest=(principal* rate* years)/200;
    printf("The value of simple Intrest is %d",simpleIntrest);

    return 0;
}